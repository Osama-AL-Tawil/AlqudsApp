<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.osamaaltawil.alquds">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:layoutDirection="rtl"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alquds">
        <activity
            android:name=".Video_details_Activity"
            android:configChanges="orientation|screenSize" />

        <activity android:name=".Videos_list_Activity" />
        <activity android:name=".Video_categories_Activity" />
        <activity android:name=".GInformations_list_Activity" />
        <activity android:name=".LandmarkDetails_Activity" />
        <activity android:name=".Post_Details_Activity" />
        <activity
            android:name=".Favorite_Activity"
            android:screenOrientation="locked" />
        <activity
            android:name=".Navigation_Activity"
            android:configChanges="orientation|screenSize|screenLayout" />
        <activity
            android:name=".Splash"
            android:screenOrientation="locked"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>